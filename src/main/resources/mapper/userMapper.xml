<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="com.hamonsoft.cportal.repository.UserRepository">

    <select id="info" resultType="Member">
        select * from tbmember
        where email=#{email}
    </select>

    <update id="chginfo">
        update tbmember set businessname=#{businessname}, membername=#{membername}, celltel=#{celltel}, businessname=#{businessname}, businessnumber=#{businessnumber}, licensegrade=#{licensegrade}
        where email = #{email}
    </update>

    <update id="chgpw">
        update tbmember set password=sha2(#{password}, 256)
        where email = #{email}
    </update>

    <update id="withdrawal">
        update tbmember set withdrawalyn = '1', withdrawaldate = #{strnow}
        where email = #{email}
    </update>


</mapper>